"""
Travel planner coordinator package.

This file marks the travel_planner directory as a Python package and exposes 
the root_agent so that it can be loaded directly as the master coordinator.
"""

# Export the coordinator agent module.
from . import agent
